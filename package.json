{
  "name": "figma-make-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "deploy:gh-pages": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/deploy-gh-pages.ps1",
    "deploy:gh-pages:timestamped": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/deploy-gh-pages.ps1 -TimestampedCommitMessage",
    "deploy": "npm run build && npm run deploy:gh-pages",
    "deploy:timestamped": "npm run build && npm run deploy:gh-pages:timestamped",
    "preview": "vite preview",
    "format": "oxfmt"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.0.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "oxfmt": "^0.2.0",
    "typescript": "^5.7.0",
    "vite": "^8.0.0"
  }
}
